# Core
ultralytics>=8.2.0      # YOLO (object detection). Pulls in torch automatically.
opencv-python>=4.9.0    # Camera capture (UVC) + drawing
numpy>=1.24

# Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
